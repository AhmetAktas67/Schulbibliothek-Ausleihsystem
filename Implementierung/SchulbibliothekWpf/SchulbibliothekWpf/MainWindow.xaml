<Window x:Class="SchulbibliothekWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchulbibliothekWpf"
        mc:Ignorable="d"
     
        Title="MainWindow" Height="800" Width="1500">

    <Window.Resources>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D6CDF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="RowBackground" Value="#F7F7F7"/>
            <Setter Property="AlternatingRowBackground" Value="#EDEDED"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
        </Style>

    </Window.Resources>





    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            
            <RowDefinition Height="*"/>
            
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>


        <!-- Obere Leiste -->

        <Grid Background="#F2F4F8" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Titel + Aktionen -->
            <DockPanel Grid.Row="0">

                <Label Content="📚 Schulbibliothek"
           FontSize="22"
           FontWeight="Bold"
           DockPanel.Dock="Left"/>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">

                    <Button x:Name="HistorieButton"
                Click="HistorieButton_Click"
                Content="Historie"/>

                    <Button x:Name="MahnungenButton"
                Content="Mahnungen"
                Background="Red"
                Foreground="White"
                Click="MahnungenButton_Click"
                Opacity="1">

                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                From="1"
                                To="0.3"
                                Duration="0:0:0.5"
                                AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>

                    </Button>

                    <Button x:Name="LogoutButton"
                Click="LogoutButton_Click"
                Content="Ausloggen"/>

                </StackPanel>
            </DockPanel>

            <!-- Suche + Verwaltung -->
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="SucheTextbox" Width="180"/>
                    <Button x:Name="Button_Suche"
                    Click="Suchen_Click"
                    Content="Suchen"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BuchHinzufügenButton"
                    Click="Buch_Hinzufügen"
                    Content="Buch +"/>

                    <Button x:Name="BuchLöschenButton"
                    Click="Buch_Löschen"
                    Content="Buch -"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Rechts Leiste -->
        <Grid Grid.Row="1" Grid.Column="1" Background="#F2F4F8" >
            <StackPanel>
                <Button x:Name="Ausleihen_Button"
                Click="Ausleihen_Button_Click"
                Content="Ausleihen"/>

                <Button x:Name="Zurückgeben_Button"
                Click="Zurückgeben_Button_Click"
                Content="Zurückgeben"/>
            </StackPanel>
        </Grid>



        <!-- INHALT Datagrid Tabelle -->
        <Grid Grid.Row="1" Margin="10">
            <DataGrid ItemsSource="{Binding Buecher}"
                      SelectedItem="{Binding SelectedBuch}"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"/>
        </Grid>
    </Grid>

</Window>
