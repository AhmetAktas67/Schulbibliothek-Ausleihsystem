<Window x:Class="SchulbibliothekWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchulbibliothekWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="850">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            
            <RowDefinition Height="*"/>
            
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>


        <!-- Obere Leiste -->

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                
                
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>


            </Grid.RowDefinitions>

            <Label   Content="Bücher" FontWeight="Bold"></Label>


            <StackPanel  Grid.Row="2" Orientation="Horizontal">
               


                <TextBox Height="20" Width="150" Grid.Row="1" x:Name="SucheTextbox"></TextBox>
                <Button Margin="10,0,0,0" Height="20" Width="120" x:Name="Button_Suche" Click="Suchen_Click"  Content="Suchen"  ></Button>

                
            </StackPanel>

            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                <Button Margin="0,0,20,0" Height="25" Width="120" Click="Buch_Hinzufügen"  Content="Buch +" ></Button>
                <Button Margin="0,0,40,0"  Height="25" Width="120" Click="Buch_Löschen"  Content="Buch -" ></Button>
            </StackPanel>
            


        </Grid>

        <!-- Rechts Leiste -->
        <Grid Grid.Row="1" Grid.Column="2">
            <StackPanel Orientation="vertical" Height="80">
                <Button x:Name="Ausleihen_Button" Click="Ausleihen_Button_Click"  Content="ausleihen" Margin="0,0,0,10"></Button>
                <Button  x:Name="Zurückgeben_Button" Click="Zurückgeben_Button_Click" Content="zurückgeben"></Button>
            </StackPanel>
        </Grid>
        
        

        <!-- INHALT Datagrid Tabelle -->
        <Grid Grid.Row="1" Margin="10">
            <DataGrid ItemsSource="{Binding Buecher}"
                      SelectedItem="{Binding SelectedBuch}"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"/>
        </Grid>
    </Grid>

</Window>
