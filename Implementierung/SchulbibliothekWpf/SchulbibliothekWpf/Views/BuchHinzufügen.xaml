<Window x:Class="SchulbibliothekWpf.Views.BuchHinzufügen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchulbibliothekWpf.Views"
        mc:Ignorable="d"
        Title="BuchHinzufügen" 
        
        Height="700" Width="550"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        
       
        
       >


    <Window.Resources>



        <Style x:Key="TextBoxWithPlaceholder"
       TargetType="TextBox"
       BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3">

                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>

                                    <TextBlock x:Name="Placeholder"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="Gray"
                                       Margin="{TemplateBinding Padding}"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder"
                                Property="Visibility"
                                Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>




        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
           
            
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D6CDF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

    </Window.Resources>






    <Grid>
        <Border Background="White"
                CornerRadius="10"
                Padding="5"
                Width="400"
               
                VerticalAlignment="Top"
                HorizontalAlignment="Center">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="150"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

               
                <TextBlock Text="📘 Buch hinzufügen"
                           FontSize="18"
                           FontWeight="Bold"
                           Grid.ColumnSpan="2"
                           HorizontalAlignment="Center"
                           Margin="10,0,0,0"/>

               
                <TextBlock  FontSize="18" Text="Titel:" Grid.Row="1"/>
                <TextBox  Style="{StaticResource TextBoxWithPlaceholder}" Tag="Titel eingeben ..."  Height="30"  x:Name="TitelBox" Grid.Row="1" Grid.Column="1"/>

                <TextBlock  FontSize="18" Text="Autor:" Grid.Row="2"/>
                <TextBox Style="{StaticResource TextBoxWithPlaceholder}" Tag="Autor eingeben ..." Height="30" x:Name="AutorBox" Grid.Row="2" Grid.Column="1"/>

                <TextBlock FontSize="18" Text="ISBN:" Grid.Row="3"/>
                <TextBox Style="{StaticResource TextBoxWithPlaceholder}" Tag="ISBN eingeben ..." Height="30" x:Name="IsbnBox" Grid.Row="3" Grid.Column="1"/>

                <TextBlock FontSize="18" Text="Jahr" Grid.Row="4"/>
                <TextBox Style="{StaticResource TextBoxWithPlaceholder}" Tag="Erscheinungsjahr eingeben ..." Height="30" x:Name="JahrBox" Grid.Row="4" Grid.Column="1"/>

                
                <Button Content="Bestätigen"
                        Click="Bestätigen_Click"
                        Grid.Row="5"
                        Grid.ColumnSpan="2"
                        Height="60"
                        Margin="0,120,0,0"
                        />
            </Grid>

        </Border>
    </Grid>
</Window>
