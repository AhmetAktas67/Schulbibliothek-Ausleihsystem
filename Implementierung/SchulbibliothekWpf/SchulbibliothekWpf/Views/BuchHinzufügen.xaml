<Window x:Class="SchulbibliothekWpf.Views.BuchHinzufügen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchulbibliothekWpf.Views"
        mc:Ignorable="d"
        Title="BuchHinzufügen" Height="800" Width="450"
        
       >


    <Window.Resources>

        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="26"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D6CDF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

    </Window.Resources>






    <Grid>
        <Border Background="White"
                CornerRadius="10"
                Padding="25"
                Width="380"
                Height="500"
                VerticalAlignment="Top"
                HorizontalAlignment="Center">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="150"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

               
                <TextBlock Text="📘 Buch hinzufügen"
                           FontSize="18"
                           FontWeight="Bold"
                           Grid.ColumnSpan="2"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

               
                <TextBlock Text="Titel:" Grid.Row="1"/>
                <TextBox x:Name="TitelBox" Grid.Row="1" Grid.Column="1"/>

                <TextBlock Text="Autor:" Grid.Row="2"/>
                <TextBox x:Name="AutorBox" Grid.Row="2" Grid.Column="1"/>

                <TextBlock Text="ISBN:" Grid.Row="3"/>
                <TextBox x:Name="IsbnBox" Grid.Row="3" Grid.Column="1"/>

                <TextBlock Text="Erscheinungsjahr:" Grid.Row="4"/>
                <TextBox x:Name="JahrBox" Grid.Row="4" Grid.Column="1"/>

                
                <Button Content="Bestätigen"
                        Click="Bestätigen_Click"
                        Grid.Row="5"
                        Grid.ColumnSpan="2"
                        Margin="0,0,0,0"
                        />
            </Grid>

        </Border>
    </Grid>
</Window>
