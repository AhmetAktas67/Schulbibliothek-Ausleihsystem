<Window x:Class="SchulbibliothekWpf.Views.HistorieFenster"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchulbibliothekWpf.Views"
        mc:Ignorable="d"
        Title="HistorieFenster" Height="450" Width="1200">



    <Window.Resources>

        
        <SolidColorBrush x:Key="HeaderBg" Color="#F2F4F8"/>

      
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

       
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D6CDF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

       
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>

       
        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="RowBackground" Value="#FAFAFA"/>
            <Setter Property="AlternatingRowBackground" Value="#EFEFEF"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

    </Window.Resources>



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>

            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>


        <!-- Obere Leiste -->

        <Grid Background="#F2F4F8"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>


            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>


            </Grid.RowDefinitions>

            <Label   Content="Historie" FontWeight="Bold"></Label>


            <StackPanel  Grid.Row="1" Orientation="Horizontal">



                <TextBox Height="30" Width="150" Grid.Row="1" x:Name="SucheTextbox"></TextBox>
                <Button Height="30"  x:Name="Button_Suche" Click="Button_Suche_Click"  Content="Suchen"  ></Button>


            </StackPanel>

           


        </Grid>


        <!-- INHALT Datagrid Tabelle -->
        <DataGrid Grid.Row="1"
          ItemsSource="{Binding Ausleihen}"
          AutoGenerateColumns="False"
          IsReadOnly="True">



            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding TageImVerzug}" Value="1">
                            <Setter Property="Background" Value="#FFFFD6D6"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>


            <DataGrid.Columns>

                <!-- Buch -->
                <DataGridTextColumn Header="Titel"
                            Binding="{Binding Buch.Titel}"
                            Width="100"/>

                <DataGridTextColumn Header="Autor"
                            Binding="{Binding Buch.Autor}"
                            Width="150"/>

                <DataGridTextColumn Header="ISBN"
                 Binding="{Binding Buch.ISBN}"
                 Width="150"/>

                <DataGridTextColumn Header="Erscheinungsjahr"
                 Binding="{Binding Buch.Erscheinungsjahr}"
                 Width="150"/>

                <DataGridTextColumn Header="Nutzer"
                    Binding="{Binding Nutzername}"
                    Width="200"/>


                <!-- Daten -->
                <DataGridTextColumn Header="Ausgeliehen am"
                            Binding="{Binding DatumAusleihe}"
                            Width="140"/>

                <DataGridTextColumn Header="Zurückgegeben am"
                            Binding="{Binding DatumRueckgabe}"
                            Width="160"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
    

</Window>

